import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aistudio.habit.tracker',
  appName: 'Habit Tracker RPG',
  webDir: 'dist'
};

export default config;
